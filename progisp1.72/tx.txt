1-Open progisp.exe
2-set the setting as shown.(make sure the program fuse is unchecked).
3-open fuse bit settings.
	CKDIV8=1	// if it is 0 the clock is divided by 8, so make it 1.
	CKSEL3=1	//
	CKSEL2=1	//Set clock to external crystal
	CKSEL1=1	//
	CKSEL0=1	//